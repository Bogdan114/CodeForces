#include <iostream>
using namespace std;
int main()
{
	int m, n;
	int otv = 0;
	cin >> m >> n;
	if (n % 2 == 0)
	{
		otv = n / 2;
		otv = otv * m;
	}
	else
	{
		otv = n / 2;
		otv = otv * m;
		otv = otv + (m / 2);
	}
	cout << otv;
	system("pause>nul");
}
