#include <iostream>

using namespace std;

int main()
{
	int a;
	int otv = 0;
	cin >> a;
	char* mas = new char[a];
	cin >> mas;
	for (int i = 0; i+1 < a; i++)
	{
		if (mas[i] == mas[i + 1])
			otv++;
	}
	cout << otv;
	system("pause>nul");
}
