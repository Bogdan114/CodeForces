#include <iostream>
#include <string>
using namespace std;
int main()
{
	string a, b;
	int j = 0;
	cin >> a;
	b = "hello";
	for (int i = 0; i < a.size(); i++)
	{
		if (a[i] == b[j])
		{
			j++;
		}
	}
	if (j == b.size())
	{
		cout << "YES";
	}
	else cout << "NO";
}
