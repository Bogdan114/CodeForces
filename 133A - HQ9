#include <iostream>
#include <string>

using namespace std;

int main()
{
	string a;
	int flag = 0;
	cin >> a;
	for (int i = 0; i < a.size(); i++)
	{
		if (a[i] == 'H' || a[i] == 'Q' || a[i] == '9')
		{
			flag = 1;
		}
	}
	if (flag)
		cout << "YES";
	else cout << "NO";
}
