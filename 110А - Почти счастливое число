#include <iostream>
#include <string>
using namespace std;

int main()
{
	string a;
	int otv=0;
	char b = '4', c = '7';
	cin >> a;
	for (int i = 0; i < a.size(); i++)
	{
		if (a[i] == b || a[i] == c)
		{
			otv++;
		}
	}
	if (otv==4 || otv==7)
		cout << "YES";
	else cout << "NO";
}
